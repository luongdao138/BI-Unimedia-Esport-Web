import { Box } from '@material-ui/core'
import React from 'react'

type GiftMemberProps = {
  type?: string
}

const GiftMember: React.FC<GiftMemberProps> = () => {
  // const classes = useStyles()

  return <Box></Box>
}

// const useStyles = makeStyles(() => ({}))

export default GiftMember
